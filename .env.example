DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require
JWT_ACCESS_SECRET=

NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_DEFAULT_TENANT_ID=
CRON_WEBHOOK_TOKEN=

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=MyPersonalTrainer ERP <no-reply@mypersonaltrainer.app>

# generar con openssl rand -base64 48 — nunca commitear la clave real.
# Cifra tenant_integrations.credentials, evolution_configs.api_key y
# payment_gateways.secret_key (ver db/migrations/027_encrypt_credentials.sql
# y docs/runbooks/rotate-pgcrypto-key.md).
PGCRYPTO_KEY=
